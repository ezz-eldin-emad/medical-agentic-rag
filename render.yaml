services:
  - type: web
    name: medical-agentic-rag-demo
    runtime: python
    plan: free
    buildCommand: uv sync --locked --no-dev
    startCommand: .venv/bin/uvicorn src.web.app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /healthz
